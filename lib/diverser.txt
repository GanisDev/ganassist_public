nu prea stiu ce e cu el


            PlatformElevatedButton(
                child: Text(
                    'Theme mode: ${PlatformTheme.of(context)?.themeMode?.name}'),
                onPressed: ()  async {
                  final mode = PlatformTheme.of(context)?.themeMode;
                  final newMode = _cycleThemeMode(mode);

                  keyValueStorage.upsertValue(key: 'themeMode', value: newMode.name);
                  PlatformTheme.of(context)?.themeMode = newMode;
                })


                ElevatedButton(
                  onPressed: () => Navigator.of(context).push(
                    platformPageRoute(
                      context: context,
                      builder: (context) => const OthersScreen(),
                    ),
                  ),
                  child: const Text('Go to the Others screen'),
                ),